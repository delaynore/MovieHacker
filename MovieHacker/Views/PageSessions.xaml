<Page x:Class="MovieHacker.Views.PageSessions"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MovieHacker.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800" Loaded="Page_Loaded" >
<!--Добавить грид и добавить сортировки-->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition MaxHeight="50" MinHeight="20"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Background="White">
            <StackPanel Orientation="Horizontal">
                <ComboBox x:Name="FilterByFilmName" Margin="10" IsEditable="True" />
            </StackPanel>
        </Grid>
        <Grid Grid.Row="1">
            <ListBox x:Name="listBox1" Grid.Row="1" ItemsSource="{Binding}"
                 SelectedItem="{Binding Id}" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid x:Name="gridelement1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition Width="auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                            </Grid.RowDefinitions>

                            <Image Grid.Column="0" Grid.RowSpan="2" HorizontalAlignment="Stretch" MaxHeight="160" MaxWidth="90" Source="{Binding Image}"/>
                            <TextBlock Grid.Column="1" Grid.Row="0" Text="{Binding MovieName}" FontSize="16" MaxHeight="30"/>
                            <StackPanel Grid.Column="1" Grid.Row="1">
                                <TextBlock Text="{Binding CinemaName}" FontSize="14"/>
                                <TextBlock Text="{Binding StartTime}" FontSize="12"/>
                                <TextBlock Text="{Binding Price}" FontSize="11"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </Grid>
</Page>
